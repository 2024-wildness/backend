# Task ID: 5
# Title: Implement Content Interaction Features (Views, Pinning, Tagging)
# Status: pending
# Dependencies: 4
# Priority: medium
# Description: Add features for user interaction with content, including tracking view counts, pinning/bookmarking items, and applying tags.
# Details:
Implement logic to increment a `view_count` on the Content entity whenever it's accessed. Implement a `is_pinned` boolean flag and APIs to pin/unpin content. Update listing queries to always show pinned items at the top. Implement tag management, allowing users to add/remove tags from content. Use a separate Tag entity and a join table for flexibility.

# Test Strategy:
Verify that accessing a content item's detail view increments its view count. Test the pin/unpin functionality and confirm that pinned items are prioritized in lists. Test adding, removing, and updating tags on a content item. Test filtering content by tags.
